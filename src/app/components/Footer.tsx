import * as React from 'react';

export default class Footer extends React.Component {
  render() {
    return (
      <section className="footer has-text-centered has-text-grey">
        @Footer content to be added
      </section>
    );
  }
}
